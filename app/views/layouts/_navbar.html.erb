<div id="navBar"></div>
<script>
  document.addEventListener('DOMContentLoaded', () => {
    VueInstances.navBar('#navBar', <%= raw({
      logoUrl: site_logo_url,
      visibleButtons: policy(:nav_bar).visible_buttons,
    }.to_json) %>)
  })
</script>
