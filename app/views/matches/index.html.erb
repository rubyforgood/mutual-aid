<%= render "layouts/view_header", resource: @matches.first %>

<table class="table table-hover table-curved table-condensed">
  <thead>
    <tr>
      <th>Owner</th>
      <th>Asker</th>
      <th>Status</th>
      <th>Notes</th>
      <th>Tentative</th>
      <th>Completed</th>
      <th>Edit</th>
    </tr>
  </thead>

  <tbody>
    <% @matches.each do |match| %>
      <tr>
        <td><%= match.owner&.name %></td>
        <td><%= match.asker&.name %></td>
        <td><%= match.status %></td>
        <td><%= match.notes %></td>
        <td><%= match.tentative %></td>
        <td><%= match.completed %></td>
        <td><%= link_to 'Edit', edit_match_path(match) %></td>
      </tr>
    <% end %>
  </tbody>
</table>
